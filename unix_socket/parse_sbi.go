package unixsocket

import (
    "fmt"
    "zaimu/types"
)

func parseSBIEmail(rawEml []byte) (*types.Transaction, error) {
    // parse Google transaction / alert format
    return nil, fmt.Errorf("SBI email parsing not implemented")
}

